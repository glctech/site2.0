{
  "name": "glctech-site-auditor",
  "private": true,
  "type": "module",
  "description": "Tooling only — the site itself has no build step (see README.md). This package.json exists solely to run auditor/run.mjs locally and in CI.",
  "scripts": {
    "audit": "node auditor/run.mjs audit",
    "audit:fix": "node auditor/run.mjs fix",
    "audit:full": "node auditor/run.mjs full"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
